#!/usr/bin/chippy

load("libboxflinger.chh");
load("libterm.chh");

func Logo()
{
	ClearScreen();
	HideCursor();

	BFDrawBox(2, 2, 7, 3, BFBOXROUNDED);

	BFDrawBox(6, 3, 7, 3, BFBOXSINGLE);

	BFDrawText(16, 5, Format("boxflinger %s\n", [BFVERSION]));

	ShowCursor();
}

Logo();
