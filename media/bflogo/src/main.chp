#!/usr/bin/chippy

load("libboxflinger.chh");
load("libterm.chh");

func Logo() {
	ClearScreen();
	HideCursor();

	BFDrawBox(6, 2, 7, 3, BFBOXROUNDED);

	BFDrawBox(2, 3, 7, 3, BFBOXSINGLE);

	BFDrawText(10, 5, Format("boxflinger %s\n", [BFVERSION]));

	ShowCursor();
}

Logo();
